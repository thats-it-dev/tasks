# Frontend template

This is a template for all frontend web-based projects. using it ensures that that each app: 

- shares the same dependencies and versions.
- shares the same aesthetic
- are a PWA
- are local first

## Getting Started

1. clone this repo
2. run `pnpm install`
3. run `pnpm dev` to get started